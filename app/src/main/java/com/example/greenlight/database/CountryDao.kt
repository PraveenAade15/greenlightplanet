package com.example.greenlight.database

import androidx.lifecycle.LiveData
import androidx.room.*
import com.example.greenlight.models.AreaResponse

//@Dao
interface CountryDao {

//    @Query("SELECT * FROM characters")
//    fun getAllCharacters() : LiveData<List<AreaResponse>>
//
//    @Query("SELECT * FROM characters WHERE ResponseData = :id")
//    fun getCharacter(id: Int): LiveData<AreaResponse>
//
//    @Insert(onConflict = OnConflictStrategy.REPLACE)
//    suspend fun insertAll(characters: List<AreaResponse>)
//
//    @Insert(onConflict = OnConflictStrategy.REPLACE)
//    suspend fun insert(character: AreaResponse)

}